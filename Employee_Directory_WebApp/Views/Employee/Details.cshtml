@* @model Employee_Directory_WebApp.Models.Employee


<h1>Employee Details</h1>

<dl class="row">
    <dt class="col-sm-3">Name</dt>
    <dd class="col-sm-9">@Model.Name</dd>

    <dt class="col-sm-3">Department</dt>
    <dd class="col-sm-9">@Model.Department</dd>

    <dt class="col-sm-3">Position</dt>
    <dd class="col-sm-9">@Model.Position</dd>

    <dt class="col-sm-3">Address</dt>
    <dd class="col-sm-9">@Model.Address</dd>

    <dt class="col-sm-3">Date of Joining</dt>
    <dd class="col-sm-9">@Model.DateOfJoining.ToShortDateString()</dd>

    <dt class="col-sm-3">CTC</dt>
    <dd class="col-sm-9">@Model.CTC.ToString("C")</dd>

    <dt class="col-sm-3">Salary</dt>
    <dd class="col-sm-9">@Model.Salary.ToString("C")</dd>

    <dt class="col-sm-3">Team Name</dt>
    <dd class="col-sm-9">@Model.TeamName</dd>

    <dt class="col-sm-3">Manager Name</dt>
    <dd class="col-sm-9">@Model.ManagerName</dd>

    <dt class="col-sm-3">Total Experience</dt>
    <dd class="col-sm-9">@Model.TotalExperience</dd>
    <dt class="col-sm-3">In-House Experience</dt>
    <dd class="col-sm-9">@Model.InHouseExperience</dd>
</dl>

<h2>Leave Details</h2>

<table class="table">
    <thead>
        <tr>
            <th>Leave Date</th>
            <th>Leave Type</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var leave in Model.Leaves)
        {
            <tr>
                <td>@leave.LeaveDate.ToShortDateString()</td>
                <td>@leave.LeaveType</td>
                <td>@leave.Status</td>
            </tr>
        }
    </tbody>
</table>

<h2>Attendance Details</h2>

<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Present</th>
            <th>Remark</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var attendance in Model.Attendances)
        {
            <tr>
                <td>@attendance.Date.ToShortDateString()</td>
                <td>@(attendance.IsPresent ? "Yes" : "No")</td>
                <td>@attendance.Remark</td>
            </tr>
        }
    </tbody>
</table>




<a href="@Url.Action("Index", "Employee")" class="btn btn-primary">Back to List</a>

<style>

</style>


<div id="parent_div">
    <div>
        background color of this div light blue
        name: @Model.Name
        position: @Model.Position
        rating : (show five star)
    </div>
    i want profile image avatar at left end half uptp top div and second half cross to below div
    <div>
        @Model.Address
        <div>
            total experiance:@Model.TotalExperience   inhouse experiance: @Model.InHouseExperience
        </div>
    </div>

    <div>
        all other details
    </div>
</div> *@

@model Employee_Directory_WebApp.Models.Employee

<div id="parent_div" style="border: 1px solid #ccc; padding: 20px; border-radius: 10px; width: 80%; marsgin: 0 auto;">

    <!-- First Section: Employee Details with Light Blue Background -->
    <div style="background-color: lightblue; padding: 20px; display: flex; position: relative;">
        <!-- Profile Image -->
        @* <div style="position: absolute; top: 0; left: 0; transform: translate(-20%, -20%);"> *@
            <div style="position: absolute; top: -105; left: 10; ">
            <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTw4xIzlTTRJKIQB1tq1Jbs5Rfj7hU6h1UtPg&s" alt="Avatar" style="border-radius: 50%; width: 100px; height: 100px; border: 5px solid white; box-shadow: 0 0 10px rgba(0,0,0,0.1);">
        </div>

        <!-- Employee Details -->
        <div style="margin-left: 180px;">
            <h2>@Model.Name</h2>
            <p><strong>Position:</strong> @Model.Position</p>
            <p>
                <strong>Rating:</strong>
                <span style="color: gold;">
                    ★★★★☆  <!-- Adjust this part dynamically if needed -->
                </span>
            </p>
        </div>
    </div>

    <!-- Second Section: Address and Experience -->
    <div style="margin-left:60px">
        <div>
            <p><strong>Address:</strong> @Model.Address</p>
        </div>
        <div style="width:100%;  display:flex ">
            <p><strong>Total Experience:</strong> @Model.TotalExperience months</p>
            <p style="margin-left:20px"><strong>In-House Experience:</strong> @Model.InHouseExperience months</p>
        </div>
    </div>

    <div style="margin-left:20px; display:flex;">
        <p><strong>Team:</strong> @Model.TeamName</p>
        <p style="margin-left:20px"><strong>Reporting Manager: @Model.ManagerName</p>
    </div>

    <div style="display:flex;">
        <div style="display: flex; background-color: lightblue; border: 2px solid; margin: 6px; padding: 10px;">
            <!-- Each child div will equally divide the space -->
            <div style="flex: 1; text-align: center; margin: 0 5px;">
                <p>Days Present</p>
                <div style="height: 50px; background-color: white; box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1); border: 2px solid #ccc; display: flex; align-items: center; justify-content: center;">
                    <p style="margin: 0;">29</p>
                </div>
            </div>

            <div style="flex: 1; text-align: center; margin: 0 5px;">
                <p>Casual Leave</p>
                <div style="height: 50px; background-color: white; box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1); border: 2px solid #ccc; display: flex; align-items: center; justify-content: center;">
                    <p style="margin: 0;">29</p>
                </div>
            </div>

            <div style="flex: 1; text-align: center; margin: 0 5px;">
                <p>Sick Leave</p>
                <div style="height: 50px; background-color: white; box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1); border: 2px solid #ccc; display: flex; align-items: center; justify-content: center;">
                    <p style="margin: 0;">29</p>
                </div>
            </div>
        </div>

        <div style="display: flex; background-color: lightblue; border: 2px solid; margin: 6px; padding: 10px;">
            <!-- Each child div will equally divide the space -->
            <div style="flex: 1; text-align: center; margin: 0 5px;">
                <p>Days Present</p>
                <div style="height: 50px; background-color: white; box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1); border: 2px solid #ccc; display: flex; align-items: center; justify-content: center;">
                    <p style="margin: 0;">29</p>
                </div>
            </div>

            <div style="flex: 1; text-align: center; margin: 0 5px;">
                <p>Casual Leave</p>
                <div style="height: 50px; background-color: white; box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1); border: 2px solid #ccc; display: flex; align-items: center; justify-content: center;">
                    <p style="margin: 0;">29</p>
                </div>
            </div>

            <div style="flex: 1; text-align: center; margin: 0 5px;">
                <p>Sick Leave</p>
                <div style="height: 50px; background-color: white; box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1); border: 2px solid #ccc; display: flex; align-items: center; justify-content: center;">
                    <p style="margin: 0;">29</p>
                </div>
            </div>
        </div>

    </div>

    <!-- Third Section: All Other Details -->
    <div style="padding: 20px;">
        <dl class="row">
            <dt class="col-sm-3">Department</dt>
            <dd class="col-sm-9">@Model.Department</dd>

            <dt class="col-sm-3">Date of Joining</dt>
            <dd class="col-sm-9">@Model.DateOfJoining.ToShortDateString()</dd>

            <dt class="col-sm-3">CTC</dt>
            <dd class="col-sm-9">@Model.CTC.ToString("C")</dd>

            <dt class="col-sm-3">Salary</dt>
            <dd class="col-sm-9">@Model.Salary.ToString("C")</dd>

            <dt class="col-sm-3">Team Name</dt>
            <dd class="col-sm-9">@Model.TeamName</dd>

            <dt class="col-sm-3">Manager Name</dt>
            <dd class="col-sm-9">@Model.ManagerName</dd>
        </dl>

        <h2>Leave Details</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>Leave Date</th>
                    <th>Leave Type</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var leave in Model.Leaves)
                {
                    <tr>
                        <td>@leave.LeaveDate.ToShortDateString()</td>
                        <td>@leave.LeaveType</td>
                        <td>@leave.Status</td>
                    </tr>
                }
            </tbody>
        </table>

        <h2>Attendance Details</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Present</th>
                    <th>Remark</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var attendance in Model.Attendances)
                {
                    <tr>
                        <td>@attendance.Date.ToShortDateString()</td>
                        <td>@(attendance.IsPresent ? "Yes" : "No")</td>
                        <td>@attendance.Remark</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Back to List Button -->
    <div style="text-align: center; margin-top: 20px;">
        <a href="@Url.Action("Index", "Employee")" class="btn btn-primary">Back to List</a>
    </div>

</div>
